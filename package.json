{
  "name": "playwright_b14",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": { "test:chromium:allure": "npx playwright test --project=chromium --workers=1 --headed && npx allure generate allure-results --clean -o allure-report && npx allure open allure-report"},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.1.0",
    "allure-playwright": "^3.3.2"
  },
  "dependencies": {
    "@faker-js/faker": "^9.9.0"
  }
}
